# Data & UI
pandas>=2.0,<3
numpy>=2.0,<3
streamlit>=1.28,<2

# Visualization (matplotlib per spec; plotly for interactive Streamlit charts)
matplotlib>=3.8,<4
plotly>=5.18,<6

# Optimization (CVXPY + OSQP solver pulled in by cvxpy)
cvxpy>=1.4,<2

# Metrics & projections (Gini, linregress, scientific helpers)
scipy>=1.11,<2

# Linear models / meta α–β tuning (scikit-optimize)
scikit-learn>=1.3,<2
scikit-optimize>=0.9,<1

# Tests (deterministic allocation checks)
pytest>=8,<9
